---
title: DTOs (Data Transfer Objects)
description: Objetos inmutables que transportan datos entre el endpoint y el caso de uso
---

Los DTOs transportan datos entre la presentación y los casos de uso sin
acoplarlos. Son dataclasses puras, sin lógica de validación ni
serialización.

Todos viven en el paquete `src/app/application/dtos/`.

## Tabla de DTOs

| DTO | Módulo | Tipo | Campos |
|---|---|---|---|
| `RegisterUserRequest` | Auth | Input | `email: str`, `password: str`, `company_name: str` |
| `LoginUserRequest` | Auth | Input | `email: str`, `password: str` |
| `RefreshTokenRequest` | Auth | Input | `refresh_token: str` |
| `AuthTokensDTO` | Auth | Output | `access_token: str`, `refresh_token: str`, `token_type: str` |
| `UserResponse` | Auth | Output | `id: str`, `email: str`, `is_active: bool`, `created_at: datetime`, `updated_at: datetime` |
| `CreateCompanyRequest` | Company | Input | `nombre: str`, `rif: str \| None`, `email_contacto: str \| None` |
| `UpdateCompanyRequest` | Company | Input | `nombre: str \| None`, `rif: str \| None`, `email_contacto: str \| None` |
| `CompanyResponse` | Company | Output | `id: str`, `nombre: str`, `slug: str`, `rif: str \| None`, `email_contacto: str \| None`, `estado: str` |
| `CreateRoleRequest` | Role | Input | `nombre: str`, `descripcion: str`, `permisos: list[PermissionDTO]` |
| `UpdateRoleRequest` | Role | Input | `nombre: str \| None`, `descripcion: str \| None`, `permisos: list[PermissionDTO] \| None` |
| `RoleResponse` | Role | Output | `id: str`, `nombre: str`, `descripcion: str`, `permisos: list[PermissionDTO]` |
| `CreateLocationRequest` | Location | Input | `parent_id: str \| None`, `nombre: str`, `tipo: str`, `descripcion: str \| None` |
| `UpdateLocationRequest` | Location | Input | `parent_id: str \| None`, `nombre: str \| None`, `tipo: str \| None`, `descripcion: str \| None` |
| `LocationResponse` | Location | Output | `id: str`, `parent_id: str \| None`, `nombre: str`, `tipo: str`, `descripcion: str \| None` |
| `CreateAssetRequest` | Asset | Input | `articulo_id: str`, `ubicacion_id: str \| None`, `serial_interno: str`, `codigo_activo: str`, `estado: str`, `fecha_adquisicion: date \| None`, `valor_monetario: float \| None`, `moneda: str` |
| `UpdateAssetRequest` | Asset | Input | `articulo_id: str \| None`, `ubicacion_id: str \| None`, `serial_interno: str \| None`, `codigo_activo: str \| None`, `estado: str \| None`, `fecha_adquisicion: date \| None`, `valor_monetario: float \| None`, `moneda: str \| None` |
| `AssetResponse` | Asset | Output | `id: str`, `articulo_id: str`, `ubicacion_id: str \| None`, `serial_interno: str`, `codigo_activo: str`, `estado: str`, `fecha_adquisicion: date \| None`, `valor_monetario: float \| None`, `moneda: str` |

## Diferencia con schemas Pydantic

Los schemas Pydantic (en `presentation/api/v1/schemas/`) validan y
serializan el formato JSON:API de la petición HTTP. Los DTOs son el
siguiente paso: datos ya validados, listos para el caso de uso.

```
HTTP request
  → Schema Pydantic (valida JSON:API, Content-Type, tipos)
    → DTO (dataclass, sin validación)
      → Caso de uso (lógica de negocio)
        → DTO (resultado)
          → Schema Pydantic (serializa JSON:API)
            → HTTP response
```

<Callout type="info">

`@dataclass(frozen=True)` hace que el DTO sea inmutable. Una vez creado,
no se puede modificar. Esto evita efectos secundarios: si el caso de uso
recibe un DTO, sabe que los datos no cambiarán durante la ejecución.

</Callout>

## ¿Por qué dataclasses y no Pydantic?

| Razón | Explicación |
|---|---|
| Sin dependencias externas | Dataclass es parte de la biblioteca estándar. El `application/` no importa Pydantic ni FastAPI. |
| Simplicidad | Un dataclass declara campos y nada más. No hay validación, ni serialización, ni configuración. |
| Inmutabilidad | `frozen=True` asegura que el objeto no cambia después de creado. |

Si el caso de uso necesita validar datos, usa value objects del dominio:

```python
# El DTO tiene str simples
dto = RegisterUserRequest(email="correo@test.com", password="Abc12345!", company_name="Mi Empresa")

# El caso de uso crea value objects que validan
email = Email(value=dto.email)           # valida formato
plain_password = PlainPassword(value=dto.password)  # valida fortaleza
```

## Próximos pasos

El [Unit of Work](./unit-of-work) es el mecanismo que los casos de uso
usan para persistir los datos recibidos a través de estos DTOs.
